# 为什么 Agent 必须是状态机

当 Agent 从一次性的问答演进为长期运行的任务系统后，状态管理就成为了核心问题。状态机并不是 Agent 的一种实现方式，而是企业级 Agent 能够稳定运行的重要基础。

## 1 前言：为什么我要讨论状态机？

在上一篇文章《什么是Agent》中，我介绍了 LLM 与 Agent 的区别，以及 Agent 的核心组成和常见设计范式。

随着 Agent 技术的发展，越来越多的框架开始支持 ReAct、Plan-and-Execute、Multi-Agent 等能力，让 Agent 从一次简单的回答，逐渐具备了自主规划和执行复杂任务的能力。

但在真正的业务实践中，我发现一个有趣的现象：
> 大多数关于 Agent 的讨论，都集中在【如何思考】，却很少讨论【如何持续执行】。

对于很多 Demo 来说，一个 Agent 的生命周期可能只有几秒钟。用户发起请求， Agent 调用几个工具，返回结果，任务便结束了。

然而，在企业级 AIGC 系统中，一个任务往往会持续几十分钟，甚至数小时。
例如在 AI 短剧系统中，一个项目需要经历剧本分析、角色提取、分镜生成、人工审核、图片和视频生成等多个阶段。期间既可能等待异步模型返回结果，也可能因为人工审核而暂停执行，甚至需要在服务重启后继续完成剩余任务。

当任务开始跨越时间、跨越多个系统，并且需要随时暂停、恢复和重试时，一个新的问题就出现了：
> Agent 如何知道自己现在执行到了哪里？下一步又应该做什么？

在我看来，这正是状态管理需要解决的问题，也是为什么越来越多的企业级 Agent 开始采用状态机设计的原因。

本文将结合实际项目经验，聊聊我为什么认为： 
> 状态机不是 Agent 的一种实现技巧，而是企业级 Agent 能够稳定运行的重要基础。

## 2 什么情况下 Agent 不需要状态 
在讨论状态机之前，我们先回答一个问题：
> 所有 Agent 都需要状态机吗？

答案是否定的。

事实上，大多数我们日常接触到的 Agent，都不需要状态机。
例如：
- 查询天气
- 翻译文本
- 总结文档
- SQL查询
- 网页搜索

这些任务都有一个共同特点：
> 一次请求，一次执行，一次返回。

整个生命周期通产只有几秒钟。
> User -> Agent -> LLM -> Tool(可选) -> Result

任务结束之后，Agent 不需要记住执行过程，也不需要关心下一次请求。
即使服务重启、请求终端，也只需要重新执行一次即可。
对于这类场景来说，Agent 更像一个“增强版 API”。
LLM 负责推理， Tool 负责执行，整个流程天然就是无状态的。

因此，为这样的 Agent 引入状态机，不仅不会带来收益，反而会增加系统复杂度。
换句话说：
> 当一个 Agent 能够在一次请求内完成时，状态管理通常不是需要有限考虑的问题。

真正的问题，出现在 Agent 不再是一次请求，而是一个持续运行的任务之后。

## 3 什么情况下 Agent 开始需要状态？
随着 Agent 能够处理越来越复杂的任务，一次请求、一轮推理的执行模式开始暴露出局限性。

还是以 AI 短剧系统为例。
假设用户上传了一部剧本，希望生成完整的短剧内容。
为了完成这个目标，Agent 可能需要执行如下流程：
> 上传剧本 -> 剧本分析 -> 角色提取 -> 分镜生成 -> Prompt生成 -> 等待审核 -> 图片生成 -> 视频生成

与前文介绍的查询天气、文档总结不同，这已经不是一个几秒钟能够完成的任务。

在整个执行过程中，可能会发生很多事情：
- 图片生成需要排队等待模型执行；
- 运营可能需要几个小时才能完成审核；
- 某个模型服务可能以你为限流而调用失败；
- Agent 服务可能因为升级或重启而中断。

如果没有状态管理，当 Agent 再次恢复运行时，它将面临一个最基本的问题：
> 我现在执行到哪里了？

例如：
- 角色是否已经提取完成？
- Prompt 是否已经审核通过？
- 图片是否已经全部生成？
- 视频生成任务是否已经提交？

如果这些信息只能保存在内存中，那么一次服务重启，就意味着整个任务需要重新开始。

显然，这既浪费计算资源，也会影响用户体验。
因此，当 Agent 从“一次请求”演进为“持续完成一个目标”时，它就需要能够记录自己的执行状态。

这里的“状态”，并不仅仅是某个变量，而是整个任务当前所处的阶段。
例如：
> CREATED -> SCRIPT_ANALYZING -> CHARACTER_EXTRACTED -> STORYBOARD_GENERATED -> WAIT_USER_REVIEW -> IMAGE_GENERATING -> VIDEO_GENERATING -> COMPLETED

无论任务暂停多久，无论服务是否重启，Agent 都能够根据当前状态恢复执行，而不是重新开始。

也正因为如此，我越来越倾向于把 Agent 看作一个不断演进状态的任务系统。

LLM 决定“下一步应该做什么”，而状态决定“当前已经做到哪里了”。

当两者结合在一起，Agent 才真正具备完成复杂任务的能力。

## 4 状态机到底解决了什么？

当 Agent 开始承担长生命周期任务后，任务管理已经成为必不可少的能力。

但状态机的价值，并不仅仅是记录当前状态。

更重要的是，它为整个任务生命周期提供了一套统一的管理方式。

## 4.1 让任务可以恢复
对于一次性的 Agent ，请求结束意味着任务结束。

但对于长生命周期任务，服务重启、网络异常、模型超时都是不可避免的。

如果没有状态记录，任务只能重新开始。

而有了状态机之后，Agent 可以根据当前状态恢复执行，而不是重复已经完成的工作。

## 4.2 让流程变得可控

随着业务不断发展，一个 Agent 往往需要支持越来越多的流程分支。

例如：
- 审核通过
- 审核驳回
- 任务取消
- 失败重试

如果这些逻辑全部散落在业务代码中，流程很容易变得混乱。

状态机将这些流转关系统一管理，让每一次状态变化都有明确的来源和去向。

## 4.3 让系统更容易扩展
假设未来短剧系统新增一个“AI 配音”环节。

如果流程完全依赖业务代码，可能需要修改多个模块。

而在状态机中，只需要增加新的状态和对应的流转关系，整个生命周期依然保持清晰。

随着业务不断演进，这种扩展能力会越来越重要。

## 4.4 让问题更容易定位
当一个 Agent 执行失败时，我们最希望知道的是：
> 它失败在什么地方？

状态机天然记录了任务当前所处的阶段。

结合日志和链路信息，开发者可以快速定位问题发生的位置，而不是从整个执行流程中逐步排查。


对于企业级 Agent 来说，状态机真正管理的并不是一个 status 字段。它管理的是任务生命周期。
从任务创建，到执行、暂停、恢复，再到最终完成，每一步都拥有明确的状态和流转规则。
也正因为如此，我更愿意把状态机理解为 Agent 的生命周期管理器，而不仅仅是一种代码实现方式。

## 5 状态机只是开始
到这里，我们已经回答了一个问题：
> 为什么企业级 Agent 需要状态机？

因为当 Agent 从一次性的问答演进为长期运行的任务系统时，它必须能够管理自己的生命周期。

而状态机，正是管理任务生命周期最自然、最可靠的方式。

不过，状态机并不是企业级 Agent 的终点。
它只是整个工程体系的基础。

例如：
- Long Running Agent 需要依赖状态机，在任务暂停后继续恢复执行；
- Human-in-the-loop 需要依赖状态机，在人工审核完成后继续推进流程；
- 失败重试需要依赖状态机，确保任务不会重复执行已经完成的步骤；
- 可观测性需要结合状态机，快速定位任务执行到了哪个阶段、为什么失败。

可以发现，这些能力看似彼此独立，但背后都有一个共同前提：
> Agent 必须清楚地知道自己当前所处的状态，以及下一步应该如何流转；

这也是为什么在很多成熟的业务系统中，状态机并不是一个可选设计，而是一项基础能力。

对于 Agent 来说，LLM 提供了只能，Tool 提供了执行能力，而状态机则将整个任务组织成一个可持续运行、可恢复、可维护的系统。

在我看来，这也是企业级 Agent 与 Demo Agent 最重要的区别之一。

## 6 总结

很多人讨论 Agent 时，关注的是模型、Prompt 或者 ReAct。

而在我的实践中，随着 Agent 从 Demo 走向生产环境，一个更加基础的问题开始浮现：

> 如何让 Agent 知道自己现在在哪里，以及下一步应该做什么。

这也是我认为状态机如此重要的原因。

它并不会让 Agent 更聪明，却让 Agent 更可靠；它不会提升模型能力，却决定了复杂任务能否持续执行。

对于企业级 Agent 来说，状态机管理的不是一个简单的 `status` 字段，而是整个任务的生命周期。

当 Agent 能够稳定地运行、暂停、恢复、重试，并与人协同工作时，它才真正具备了走向生产环境的能力。

而状态机，正是这一切的起点。
