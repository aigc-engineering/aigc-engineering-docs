# Memory 是企业级 Agent 的长期记忆
> Memory 的价值，并不是让 Agent 记住更多，而是让 Agent 能够基于过去持续完成未来的目标。

## 前言
在上一篇文章中，我们讨论了 Human-in-the-loop，以及 Agent 如何在关键决策点主动引入人的参与。

随着 Long Running、Checkpoint 和 Human-in-the-loop 等能力逐渐完善，Agent 已经具备了持续完成复杂任务的能力。

但还有一个问题始终存在：

**当 Agent 再次开始执行任务时，它如何知道过去发生了什么？**

如果每一次执行都需要重新理解业务、重新分析上下文、重新建立用户偏好，那么 Agent 虽然能够持续运行，却始终停留在"第一次工作"的状态。

因此，企业级 Agent 不仅需要管理任务的生命周期，也需要持续管理任务过程中积累的信息。

这就是 Memory 存在的意义。

## 1. 什么是 Memory？
很多人第一次接触 Agent Memory 时，都会把它理解成"聊天记录"或者"上下文缓存"。

这种理解并不完整。

对于企业级 Agent 来说，Memory 保存的并不是一次对话，而是 **未来执行任务仍然需要利用的信息。**

例如，在 AI 短剧系统中，Agent 可以记住：
- 用户确认的角色形象；
- 已确定的世界观设定；
- 故事中的人物关系；
- 用户偏好的画风；
- 已经确认的 Prompt 风格。

这些信息不会因为一次任务结束而失去价值。

相反，它们会持续影响后续的决策。

因此，Memory 并不是记录过去，而是帮助 Agent 在未来做出更加一致的决策。

## 2. 为什么企业级 Agent 需要 Memory？
对于传统程序来说，每一次请求都是一次新的开始。

请求结束后，执行过程随之结束，下一次请求重新开始计算。

但 Agent 不同。

Agent 的目标往往具有连续性。

例如，在一个持续迭代的短剧项目中，昨天已经确认了角色形象，今天继续生成分镜，明天开始制作视频。

如果 Agent 每一次都重新分析角色、重新理解风格、重新生成 Prompt，那么整个项目很容易出现前后不一致的问题。

真正需要保持连续性的，并不是一次推理，而是整个业务目标。

因此，Memory 并不是为了减少模型调用，而是为了保证 Agent 在不同阶段都能够基于同一份业务事实持续推进目标。

从工程角度来看，Memory 保证的是业务连续性，而不仅仅是上下文连续性。

## 3. 什么信息应该成为 Memory？
Memory 最大的挑战，从来不是"如何存"，而是**存什么**。

如果把所有对话、所有工具调用、所有推理过程都保存下来，Agent 确实拥有了大量历史数据，但这些数据并不会自动转化为有效的 Memory。

真正的 Memory，不应该只是历史记录，而应该是**未来仍然能够影响决策的信息。**

也就是说，一个信息是否值得成为 Memory，不取决于它是否发生过，而取决于它是否还会影响未来的任务。

例如，在 AI 短剧系统中，下面这些信息就具有长期价值：

- 用户偏好的画风，例如写实、国风、二次元；
- 已经确认的角色形象和人物关系；
- 剧本世界观、时代背景等基础设定；
- 已确认的 Prompt 风格或生成策略；
- Agent 在多次尝试后总结出的有效经验。

这些信息不会因为一次任务结束而失效，相反，它们会持续影响后续每一次决策，因此非常适合作为 Memory 保存下来。

与此同时，还有一类信息同样值得长期保留，那就是**业务事实（Business Facts）**。

例如：

- 女主角已经确定为短发形象；
- 第一集已经完成并发布；
- 第三集不能修改人物设定；
- 当前项目采用电影级写实风格。

这些事实并不是模型推理得到的结论，而是业务已经确认的结果。

未来无论哪个 Agent 接手任务，都应该基于这些事实继续工作，而不是重新分析、重新确认。

除了业务事实之外，Agent 在执行过程中积累的经验，也可以逐渐沉淀为 Memory。

例如，同一种图片风格经过多次尝试后，Agent 发现某种 Prompt 组合能够获得更好的效果；或者某个模型在特定场景下成功率更高。这些经验虽然不是业务数据，却能够持续提升 Agent 后续执行任务的质量和效率。

当然，并不是所有信息都应该进入 Memory。

例如：

- 每一次聊天内容；
- 每一次 Tool 调用；
- 每一步模型推理；
- 临时变量或中间计算结果。

这些数据更适合作为日志或运行记录，它们对于恢复任务、排查问题很有价值，但并不会长期影响 Agent 的决策。

如果全部保存为 Memory，不仅会增加管理成本，也会让真正重要的信息被大量噪声淹没。

因此，在工程实践中，我更倾向于使用一个简单的判断标准：

> 如果删除这条信息，未来的 Agent 是否会做出不同的决策？

如果答案是**会**，那么它很可能应该成为 Memory。

如果答案是**不会**，那么它更适合作为日志、上下文或者运行数据，而不是长期记忆。

从这个角度来看，Memory 并不是一个数据仓库，也不是聊天记录。

**它更像是 Agent 在不断完成任务过程中沉淀下来的长期知识。**

只有这些真正具有持续价值的信息，才能帮助 Agent 在未来的任务中保持一致性，并不断积累经验，而不是每一次都从零开始思考。

| 建议作为 Memory | 原因          |
| ----------- | ----------- |
| 用户长期偏好      | 持续影响后续决策    |
| 世界观、角色设定    | 保证内容一致性     |
| 已确认的业务事实    | 避免重复确认和重复推理 |
| Agent 总结的经验 | 提升后续执行效率    |
| 长期配置与规则     | 保持系统行为稳定    |

| 不建议作为 Memory | 更适合存放的位置                 |
| ------------ | ------------------------ |
| 全量聊天记录       | Conversation Context 或日志 |
| 每一次 Tool 调用  | 日志（Log）或 Trace           |
| 每一步推理过程      | Trace 或 Observability    |
| 临时变量、中间结果    | Runtime Context          |
| 当前执行进度       | Checkpoint               |



## 4. Memory 与 Checkpoint 有什么区别？
Memory 和 Checkpoint 经常一起出现，因此也最容易被混淆。

Checkpoint 关注的是：

> 任务执行到了哪里。

它保存的是当前状态、执行进度以及恢复执行所需要的信息。

而 Memory 关注的是：

> Agent 已经知道了什么。

它保存的是那些能够持续影响未来决策的信息。

例如，在 AI 短剧系统中：

Checkpoint 可能记录：

- 当前正在生成角色图片；
- 已提交的图片生成任务 ID；
- 下一步等待人工确认。

而 Memory 则可能保存：

- 导演偏好写实风格；
- 女主角采用短发形象；
- 第一集已经确定主要人物关系。

可以发现，两者回答的是完全不同的问题。

Checkpoint 负责恢复任务。

Memory 负责延续经验。

一个关注执行过程，一个关注业务知识。

它们共同支撑了 Long Running Agent 的连续运行。

## 5. Memory 如何融入 Agent Runtime？
随着 Agent 生命周期不断延长，它所管理的不再只是执行过程，也包括越来越多长期积累的信息。

因此，Memory 逐渐成为 Agent Runtime 的核心组成部分。
```
                 Agent Runtime
                       │
      ┌────────────────┼────────────────┐
      ▼                ▼                ▼
    State         Checkpoint         Memory
      │                │                │
      └────────────────┼────────────────┘
                       ▼
                Long Running Agent
```
其中：

- State 描述 Agent 当前所处的位置；
- Checkpoint 保证 Agent 可以恢复执行；
- Memory 保证 Agent 能够延续过去的知识。

三者共同构成了 Agent 生命周期管理的重要基础。

当 Agent 再次开始执行任务时，它不仅知道自己应该从哪里继续，也知道过去已经积累了哪些经验，以及这些经验如何影响未来的决策。

从这个角度来看，Memory 并不是 Agent 的附加能力，而是企业级 Agent 持续完成复杂目标的重要基础。


## 总结
随着 Agent 从一次请求演进为 Long Running Agent，它所需要管理的已经不仅仅是执行过程。

State、Checkpoint 和 Memory 分别解决了三个不同的问题：

- State：我现在处于哪个阶段？
- Checkpoint：如果任务中断，我应该如何继续？
- Memory：过去积累的信息，如何帮助我完成未来的目标？

它们共同构成了 Agent Runtime 的基础能力，也让 Agent 从一次性的任务执行者，逐渐演进为能够持续完成目标的长期协作者。

在我看来，Memory 的真正价值，并不是让 Agent 记住更多内容，而是让它能够在每一次新的执行中，延续过去的经验，保持目标的一致性。