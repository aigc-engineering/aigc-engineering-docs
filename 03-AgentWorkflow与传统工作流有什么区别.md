# Agent Workflow 与传统工作流有什么区别？
> 为什么企业不会用 Agent 替代 Workflow，而是让它们协同工作

## 1 前言
随着 Agent 技术的发展，一个观点开始频繁出现：
> Agent 将取代传统 Workflow。

不少 Agent Framework 也开始提供可视化编排、任务调度、状态管理等能力，看起来与传统工作流系统越来越相似。

于是，一个问题也随之而来：
> Agent Workflow 和传统 Workflow，到底有什么区别？

如果只是从功能上看，两者似乎都可以定义执行流程、调用工具、管理任务状态，甚至都支持条件分支和异常处理。

但在实际项目中，我发现它们解决的并不是同一个问题。

在 AI 短剧系统中，我们依然使用 Workflow 来编排整个业务流程，例如创建项目、提交异步任务、等待审核以及最终发布。而在 Workflow 的某些节点，我们引入 Agent 来完成剧本分析、内容生成、只能决策等工作。

换句话说，Workflow 和 Agent 并不是相互替代的关系，而是承担着不同的职责。

在我看来，它们最大的区别并不在于是否能够编排流程，而在于：
> Workflow 关注的是流程如何执行，Agent 关注的是目标如何完成。

理解这一点，也就理解了为什么企业级 AI 系统并没有放弃 Workflow，而是在 Workflow 的基础上，引入了 Agent。

## 2 传统 Workflow 在解决什么问题？
在讨论 Agent Workflow 之前，我们先回到一个更基础的问题：
> 为什么软件系统需要 Workflow？

在企业应用中，很多业务本身就是由一系列固定步骤组成的。

例如，一个订单系统可能会经历：
> 创建订单 -> 支付 -> 库存扣减 -> 发货 -> 订单完成

又或者，在 AI 短剧系统中，一个项目通常会经历：
> 创建项目 -> 上传剧本 -> 剧本解析 -> 资产生成 -> 审核发布

这些流程都有一个共同特点：
> 执行顺序是预先确定的。

开发者在设计系统时，就已经定义好了每一步应该做什么，以及下一步应该进入哪里。

Workflow 的职责，就是按照这些规则稳定地推进整个流程。

因此，一个成熟的 Workflow 系统通常具备以下能力：
- 流程编排：按照预定义的顺序执行任务；
- 状态流转：记录任务当前所处的阶段；
- 异常处理：支持失败重试、超时处理和流程终止；
- 可观测性：能够追踪流程执行情况，方便定位问题。

可以发现，Workflow 更关注的是：
> 流程是否按照预期执行。

至于每一个节点内部是如何完成任务的，Workflow 并不关心。

例如，在“剧本解析”这个节点，Workflow 只需要知道任务是否执行成功，以及何时进入下一步。至于剧本应该如何分析、是否需要补充信息、是否需要重新推理，这些都不属于 Workflow 的职责。也正是因为如此，传统 Workflow 非常适合处理规则明确、流程稳定的业务。

但当每一步开始需要根据上下文自主决策时，Workflow 的优势，也逐渐变成了它的局限。

## 3 Agent Workflow 为什么出现？

传统 Workflow 的优势，在于流程时确定的。每一个节点应该做什么、什么时候结束、下一步进入哪里，开发者都可以提前定义。

但随着 AI 能力逐渐融入业务，一个新的问题出现了：
> 有些节点，已经无法用固定规则来描述。

还是以 AI 短剧系统为例。在传统 Workflow 中，我们可以定义这样一条流程：
> 剧本解析 -> 角色提取 -> 分镜生成 -> 图片生成

对于 Workflow 来说，每一个节点都只是一个待执行的任务。至于节点内部如何完成，Workflow 并不关心。

但当“剧本解析”交给 LLM 之后，情况开始发生变化。例如：
- 剧本信息不足，是否需要补充上下文？
- 角色关系冲突，是否需要重新分析？
- Prompt 质量不佳，是否需要重新生成？
- 图片风格不一致，是否需要重新选择生成策略？

这些决策，很难在设计 Workflow 时全部提前定义。因为它们依赖于模型推理结果、工具执行结果以及当前上下文。
也就是说，下一步应该做什么，不再由开发者决定，而是需要系统根据当前情况动态判断。

这正是 Agent 发挥作用的地方。

相比于 Workflow 关注“流程如何执行”，Agent 更关注：
> 为了完成目标，下一步最应该做什么。

因此，Agent 并不是替代 Workflow。它更像是 Workflow 中一个具备自主决策能力的节点。

Workflow 负责推进整个业务流程，而 Agent 则负责完成那些无法通过固定规则描述的复杂任务。

从这个角度来看，Workflow 与 Agent 并不是竞争关系，而是分别解决不同层次的问题。

## 4 Workflow 和 Agent 的本质区别

经过前面的分析，可以发现：Workflow 和 Agent 都能够组织任务、调用工具、管理状态。但它们关注的重点并不相同。

在我看来，两者最大的区别可以概括为一句话：
> Workflow 管理流程，Agent 管理决策。

Workflow 的核心职责，是确保任务按照既定流程稳定执行。例如：
> 创建项目 -> 上传剧本 -> 剧本解析 -> 审核发布

这里的流程在系统设计时就已经确定。Workflow 关心的是：
- 当前执行到哪一步？
- 下一步应该进入哪里？
- 如果执行失败，应该如何恢复？

整个流程具有较强的确定性。

而 Agent 则不同，它并不关心固定的执行路径，而是围绕目标不断做出决策。例如，在“剧本解析”这个阶段，Agent 可能会思考：
- 当前信息是否足够？
- 是否需要重新提取角色？
- 是否需要调用知识库补充背景？
- 是否应该调整 Prompt 后重新生成？

这些决策，并不是开发者提前写好的流程，而是 Agent 根据当前上下文自主完成的。因此，两者解决的是两个不同层次的问题。

Workflow 决定：任务如何流转。

Agent 决定：为了完成目标，下一步应该做什么。

这也是为什么 Agent 并没有取代 Workflow。

对于企业级 AI 系统来说，一个完整的业务流程依然需要 Workflow 来负责调度和治理，而 Workflow 中那些需要智能决策的节点，则交由 Agent 来完成。
两者各司其职，共同完成整个业务目标。

从工程实践来看，我更倾向于把它们理解为一种协作关系，而不是替代关系。

Workflow 提供稳定、可控的执行框架，Agent 提供灵活、自主的决策能力。当两者结合时，系统既能够保持工程上的稳定性，又能够发挥大模型带来的智能能力。

## 5 企业真正采用的 Workflow + Agent

理解了 Workflow 与 Agent 的职责之后，一个问题也就有了答案：
> 企业应该选择 Workflow，还是 Agent？

在我看来，这并不是一道选择题。

在实际项目中，我们并没有因为引入 Agent 而放弃 Workflow，而是让两者分别承担自己擅长的工作。

例如，在 AI 短剧系统中，一个项目的整体流程依然由 Workflow 管理：
> 创建项目 -> 上传剧本 -> 剧本解析 -> 等待审核 -> 图片生成 -> 视频生成 -> 项目完成

Workflow 负责的是整个业务流程的推进，包括任务调度、状态流转、失败重试以及人工审核等工程能力。

而当流程进入某个需要智能决策的节点时，则交由 Agent 接管。

例如，在“剧本解析”阶段，Agent 并不是简单调用一次 LLM，而是可能经历：
> 分析剧本 -> 提取角色 -> 检查角色完整性 -> 补充上下文 -> 生成最终结果

这个过程如何执行，并不是 Workflow 提前定义好的，而是 Agent 根据当前上下文自主完成。当 Agent 完成自己的目标后，再将结果交还给 Workflow，继续推进后续业务流程。整个流程更像是一种协作关系：
> Workflow --> Agent --> Workflow

Workflow 提供稳定的流程治理能力，Agent 提供灵活的智能决策能力。
两者各自关注不同的问题，也因此能够很好地协同工作。

随着 Agent 能力不断增强，未来 Workflow 中可能会出现越来越多的 Agent 节点。但无论 Agent 如何发展，Workflow 在流程治理、任务调度、状态管理等方面的价值依然存在。

至少在可以预见的一段时间内，企业级 AI 系统更可能的发展方向，不是 “Workflow 被 Agent 替代”，而是 “Workflow 与 Agent 深度融合”。

## 6 总结

Workflow 和 Agent 并不是替代关系，而是企业级 AI 系统中的两个重要组成部分。
```markdown
                   User
                     │
                     ▼
              Workflow Engine
                     │
     ┌───────────────┼───────────────┐
     ▼               ▼               ▼
  普通任务        Agent Task        普通任务
                     │
                     ▼
              Agent Runtime
                     │
         ┌───────────┴───────────┐
         ▼                       ▼
        LLM                   Tools
```
> Workflow 负责整个业务流程
> Agent Runtime 负责完成复杂任务。

两者通过清晰的边界协同工作。

下一篇，我们将继续讨论：为什么真正的 Agent，一定是 Long Running 的？