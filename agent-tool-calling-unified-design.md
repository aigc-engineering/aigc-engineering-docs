# Agent 的工具调用怎么做：从 Function Calling、Skill Tool 到 MCP 适配的统一设计

做 Agent 时，大家很容易把注意力集中在一个问题上：模型会不会调工具。

但工程里真正难的，通常不是“会不会调”，而是“怎么把不同来源的工具、不同厂商的 function calling 协议，以及多轮工具循环，收敛成一个稳定的执行框架”。

我最近在一个 Go 项目里梳理了一套比较完整的工具调用结构。它有几个很典型的特点：

- 支持本地函数工具
- 支持把 skill 作为一种特殊工具
- 支持通过 MCP 动态加载外部工具
- 模型侧统一走 OpenAI-compatible 的 `tools` / `tool_calls` 协议
- 执行侧同时覆盖单轮调用和流式多轮 tool loop

这篇文章不讲 prompt 技巧，重点讲工程设计：Agent 的工具调用到底应该怎么分层。

## 一、先把概念拆开：Tool Calling 不等于 Function Calling

这是最容易混淆的一点。

`tool calling` 是 Agent 层概念，说的是模型在推理过程中，决定是否借助外部能力完成任务。这里的“工具”可以是数据库查询、HTTP 接口、搜索、代码执行，甚至可以是“加载某个技能说明”。

`function calling` 则是模型 API 层协议。它描述的是：模型如何把“我要调用哪个工具、参数是什么”这件事，以结构化格式返回给调用方。

换句话说：

- Tool 是能力抽象
- Function Call 是承载工具调用意图的一种协议
- MCP 是一种工具暴露和分发协议
- Skill Tool 是一种特殊工具，它不直接执行业务，而是给模型补充操作说明

如果一开始不把这几个层次拆开，后面系统一复杂，代码就会纠缠在一起。

## 二、Agent 的核心不是“调用工具”，而是“统一工具协议”

在真实项目里，工具调用通常同时面对三类异构性。

第一类，是工具来源异构。你的工具可能来自：

- 本地函数
- 外部进程
- MCP server
- 内部服务 API
- 技能库

第二类，是模型协议异构。

虽然很多厂商都说自己兼容 OpenAI，但真正落地时你会发现差异很多，比如：

- 是否支持 `tools`
- 是否支持 `tool_choice`
- 流式时 `tool_calls` 是不是分片返回
- `finish_reason` 的行为是否一致
- `temperature`、`max_tokens` 等字段是否兼容

第三类，是执行链路异构。

有的场景只是让模型做一次工具选择；有的场景需要模型边输出边调工具；有的场景一轮会并发调用多个工具；有的场景还要把工具结果继续喂回模型，进入下一轮。

所以，Agent 的关键设计，不是绑定某一家模型，也不是写多少 prompt，而是先建立一个统一的工具协议层，让“工具定义”“工具执行”“协议适配”“多轮编排”彼此解耦。

## 三、从代码里抽象出的第一层：统一 Tool 接口

这套设计里，最重要的一个收敛点，是统一的 `Tool` 接口。它本质上只做两件事：

- 暴露工具定义
- 执行工具调用

典型接口大概是这样：

```go
type Tool interface {
    GetDefinition() openai.Tool
    Run(ctx context.Context, args string) (string, error)
}
```

这个抽象非常关键，因为它把一个工具拆成了两部分职责。

`GetDefinition()` 面向模型，负责告诉 LLM：

- 工具名是什么
- 描述是什么
- 参数 schema 是什么

`Run()` 面向执行器，负责在模型真正发起调用后，拿参数并返回结果。

很多工具系统不稳定，问题就在于这两层没拆开。模型看到的 schema 和真实执行逻辑纠缠在一起，后面一旦要接新协议，改动面就会很大。

在这个接口之上，再用一个 `ToolManager` 做注册和分发：

- `Register(name, tool)`
- `GetAllToolsSchema()`
- `ExecuteToolCall(ctx, call)`

这就形成了一个很清晰的结构：所有异构工具，先适配成统一 `Tool`，然后统一交给 manager 暴露给模型。

## 四、三种典型的 Tool Calling 适配方式

### 1. 本地函数工具适配

这是最直接的一类。

比如一个 `get_weather` 工具，可以直接在进程内实现，然后导出成标准 `FunctionDefinition`。模型只看到一个普通 function tool，执行层则直接调用本地代码。

这种方式适合：

- 延迟要求高的工具
- 内部稳定能力
- 参数和返回值都比较明确的操作

它的优点是简单、快、可控；缺点是工具生态扩展性一般，新的能力通常要改主进程代码。

### 2. Skill 适配为工具

这套代码里有一个很有意思的设计：`load_skill`。

它不是典型意义上的业务工具。它不查数据库，不调外部 API，也不直接生成最终结果。它做的事情是：让模型在必要时，先加载一个技能说明，再按技能说明继续完成任务。

这类工具的本质，其实是“上下文注入器”。

它解决的不是“模型没有执行能力”，而是“模型不知道该按什么操作规程做”。

例如某些复杂任务：

- 写特定格式的方案
- 按某种工作流处理数据
- 遵循某套领域 SOP 输出内容

这些任务未必需要新后端接口，但模型需要先获得一段可靠的操作指令。这时把 skill 包装成一个工具，往往比把大段说明直接塞进 system prompt 更灵活。

因为它有几个明显优势：

- skill 可以按需加载，而不是每轮都塞进上下文
- skill 可以枚举和治理，形成能力目录
- skill 可以和其他工具一起参与同一个 tool loop

从 Agent 架构角度看，这是一个值得强调的观点：不是只有“执行器”才叫工具，凡是能改变模型决策质量的外部能力，都可以被视为工具。

### 3. MCP 工具适配

第三类是通过 MCP 动态接入外部工具。

它的做法是：

1. 连接 MCP server
2. 调用 `ListTools`
3. 拿到每个 MCP tool 的描述和 input schema
4. 包装成 OpenAI-compatible `function tool`
5. 注册进同一个 `ToolManager`

这样一来，模型侧完全不需要知道这个工具来自哪里。对模型来说，它只是在调用一个名为 `mcp__xxx` 的 function；对执行器来说，真正的调用会被转发到 MCP session。

这类适配的价值非常大，因为它把“工具来源扩展”从“改主程序代码”变成了“接一个新 server”。

我认为 MCP 在 Agent 体系里的价值，不是又多了一种工具协议，而是它提供了一个很好的“工具生态接入层”：

- 工具可以动态发现
- schema 可以自动透出
- 执行可以隔离在独立进程或独立服务
- 主 Agent 不需要知道工具内部实现细节

如果你的 Agent 未来会接越来越多系统，MCP 这一层很值得保留。

## 五、第二层适配：模型协议适配，比工具适配更容易踩坑

很多项目在工具层做得还行，但在模型协议层踩得很深。

原因很简单：工具来源再复杂，最终都要喂给模型；而模型厂商之间虽然表面兼容，细节却经常不一致。

一个比较稳妥的做法，是在工具层之外，再定义一层 vendor-neutral 的工具结构，比如：

```go
type ToolDef struct {
    Name        string
    Description string
    Parameters  map[string]any
}
```

业务层、intent 层、编排层只依赖 `ToolDef`，而真正调用某家 LLM 时，再把 `ToolDef` 翻译成对应 SDK 的 `tools` 请求结构。

这层抽象的价值在于：

- 工具定义不和某个 SDK 强绑定
- 替换模型厂商时，只需要改协议适配层
- 可以针对不同模型做兼容性兜底，而不污染业务层

这其实是 Agent 工程里一个很重要的原则：工具应当是 vendor-neutral 的，模型 SDK 只是一个“协议翻译器”。

## 六、LLM 的 Function Calling 不是一种，而是几种

很多人说“我们支持 function calling”，但工程上这句话信息量太低了。因为不同种类的 function calling，对 Agent 架构影响很大。

### 1. 单轮非流式 function calling

这是最简单的一种。

发起一次请求，模型返回完整的 `tool_calls`，你解析工具名和参数，执行后结束，或者再补一轮。

它适合：

- 意图分类
- 工具路由
- 轻量决策
- 单步任务分发

这类模式实现简单，稳定性也最好。很多 Agent 系统的第一版都应该从这里起步。

### 2. 流式 function calling

这是复杂度明显上升的一类。

在流式场景里，模型返回的 `tool_calls` 往往不是一次性给全，而是增量分片返回。你可能先收到：

- tool call 的 index
- 再收到 id
- 再收到 function name
- 参数 JSON 则是一段一段拼出来的

这意味着你不能在收到第一段工具调用时就立刻执行，也不能假设 `arguments` 总是完整 JSON。

必须维护一层“按 index 累积 tool_call delta”的拼接逻辑，直到本轮流结束后，才能拿到完整调用。

这也是流式 tool calling 最容易写错的地方。

### 3. 单工具调用模型

有些模型虽然支持 function calling，但行为上更接近“每轮最多稳定给一个工具”。这种模型实现简单，适合做路由器，但在复杂任务上吞吐较低。

### 4. 多工具并行调用模型

另一些模型一轮可能给出多个 `tool_calls`。这时候系统要处理的不只是执行，还包括：

- 调用顺序
- 是否允许并行
- 结果聚合方式
- 工具间依赖关系
- 是否需要失败重试

你的执行框架如果一开始就假设“一轮只有一个工具”，后面扩展时会比较痛苦。

### 5. 强 schema 约束型与弱约束兼容型

还有一类差异，不在是否支持工具，而在“支持得有多严格”。

有的模型对 JSON schema 遵守得比较好，参数结构稳定；有的模型号称兼容，但参数经常漂移，甚至会混入自然语言说明。

工程上这两类模型的接法不一样：

- 强约束模型更适合自动执行
- 弱约束模型更需要参数校验、容错解析和失败回退

所以“支持 function calling”只是起点，不是终点。真正影响系统设计的，是它支持的是哪一种。

## 七、多轮 Tool Loop，才是 Agent 工具调用的核心战场

真正的 Agent，一般不会停留在“模型选一个工具”这一步。它更常见的模式是：

1. 用户发起请求
2. 模型决定调用工具
3. 系统执行工具
4. 把工具结果回填给模型
5. 模型基于结果继续推理
6. 如果还需要，再继续调用下一个工具
7. 直到输出最终答案

这就是 tool loop。

在工程实现里，这个循环有几个关键点。

第一，模型消息必须按原生工具协议回填。

也就是：

- assistant 消息里带上本轮完整的 `tool_calls`
- 每个工具结果用独立的 `tool` 消息返回
- `tool_call_id` 要和模型原始调用一一对应

这一步不能省，也不能“看起来差不多就行”。

因为从模型视角看，它并不知道你的后端做了什么。它只知道自己上一轮发出了哪些工具调用，以及后面收到了哪些 tool result。如果你不按协议回填，模型就会在下一轮丢失上下文，不承认自己调过工具，甚至重复发同样的调用。

第二，tool loop 必须有轮次上限。

任何允许模型自主调用工具的系统，都必须防止无限循环。常见原因包括：

- 模型误判工具结果
- 工具结果不满足预期，模型反复尝试
- schema 不清楚，模型一直换参数重试

所以像 `maxToolRounds` 这样的上限是很必要的。它不是优化项，而是保护机制。

第三，执行器最好由外部注入。

也就是工具调用循环本身不关心某个工具怎么实现，只需要一个：

```go
exec func(name, argsJSON string) string
```

这样 tool loop 负责协议编排，工具运行则交给外部执行层。这种拆法会让测试、替换、扩展都简单很多。

## 八、流式 Tool Calling 里最容易被低估的几个坑

如果说单轮 function calling 主要是协议解析问题，那流式 tool loop 更像一个状态机问题。

最容易踩坑的地方通常有四个。

### 1. `tool_calls` 是增量拼接的

你收到的不是完整调用，而是碎片。尤其 `arguments` 很可能是分多段到达的 JSON 字符串。必须做累积，而不是覆盖。

### 2. `finish_reason=tool_calls` 不代表可以立刻停止读流

有些实现一看到 `tool_calls` 就 break，结果后面的 delta 或 `[DONE]` 没正常消费，状态容易错乱。更稳的做法是继续把整轮流读完，再进入工具执行。

### 3. 一轮里可能既有内容输出，也有工具调用

模型可能先输出一些解释性内容，再发起工具调用。这时前端展示和后端执行如何协调，就需要提前设计好。不能简单把“有工具调用”视为“这一轮没有文本输出”。

### 4. 调试信息必须足够细

工具调用链路出了问题，如果没有调试日志，几乎很难定位。建议至少打印：

- 本轮发送给模型的 messages
- 流式收到的原始 tool call delta
- 拼接后的完整工具名和参数
- 执行结果摘要
- 回填后的消息结构

这不是为了“看着安心”，而是因为多厂商兼容时，问题通常就出在这些细节上。

## 九、为什么 Skill Tool 很重要，但常被忽略

很多工程团队在做 Agent 时，会把工具理解成“能查数据、能执行动作的东西”。这个理解不算错，但不完整。

还有一类能力，不能直接帮模型完成任务，却能显著提升模型完成任务的方式，那就是 Skill。

把 skill 设计成工具，而不是直接塞进系统提示词，有几个很现实的好处：

- 节省上下文，按需加载
- 让模型自己决定何时需要某个技能
- skill 可以被统一治理、版本化和扩展
- skill 可以作为工具链中的一环，参与多轮编排

从工程视角看，这相当于把“prompt 资产”也纳入了工具体系。

这件事很重要，因为 Agent 的很多能力，未必来自“更多 API”，而是来自“更可组合的操作知识”。

## 十、一个更稳的分层方式

如果把上面的经验总结成一个更通用的架构，我会建议把 Agent 工具调用拆成四层。

第一层，工具定义层。

负责统一工具的 `name`、`description`、`parameters`、`run` 语义，不关心来源，也不关心模型厂商。

第二层，工具来源适配层。

把不同来源的能力适配成统一工具，例如：

- 本地函数工具
- skill 工具
- MCP 工具
- HTTP 工具

第三层，模型协议适配层。

把 vendor-neutral 的工具定义，翻译成某个模型 SDK 需要的请求结构；同时把模型返回的 tool call 解析成内部统一格式。

第四层，Agent 编排层。

负责：

- 单轮调用
- 多轮 tool loop
- 流式输出
- 工具结果回填
- 轮次控制
- 错误处理

这四层分开以后，整个系统会变得清晰很多。你以后换模型、换工具来源、换执行方式，都不会牵一发动全身。

## 十一、几个工程上的结论

如果让我把这篇文章压缩成几个结论，我会保留下面这些。

第一，不要把 tool calling 当成 prompt 技巧，它首先是协议设计问题。

第二，工具来源一定会越来越杂，所以越早建立统一 `Tool` 抽象越好。

第三，Skill 应该被视为工具体系的一部分，而不是散落在各处的 prompt 片段。

第四，MCP 很适合作为工具生态接入层，因为它天然适合动态发现和跨进程隔离。

第五，真正决定 Agent 稳定性的，不是模型“能不能调工具”，而是下面四件事：

- schema 是否统一
- tool call delta 是否能正确拼接
- tool result 是否按原生协议正确回填
- 多模型兼容层是否足够薄、足够可替换

## 结语

很多人以为 Agent 的进化路径，是模型越来越聪明，所以工具调用会越来越简单。

但从工程角度看，恰好相反。

模型越强，接入的工具越多，调用链越长，异构性越高。这时候真正拉开差距的，不是某个 prompt，也不是某个模型名字，而是你能不能把本地函数、技能知识、MCP 工具和不同厂商的 function calling 差异，统一收敛到一个稳定的 tool loop 里。

我自己的判断是：

未来 Agent 的核心竞争力，不在于有没有 function calling，而在于能不能把“工具能力”和“模型协议”彻底解耦，然后建立一个稳定、可扩展、可观测的工具调用系统。
